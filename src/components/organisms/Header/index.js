import styled from 'styled-components'

import theme from '../../../theme'

const Header = styled.div`
  background-color: ${theme.colors.shades[3]};
  height: 56px;
  border-bottom: 2px solid ${theme.colors.shades[0]};
`

export default Header
